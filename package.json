{
  "scripts": {
    "buildall": "lerna run build --since",
    "server": "lerna run run:sockets --stream"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "chaingun/packages/*"
  ],
  "name": "notabug-workspace",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
