{
  "scripts": {
    "buildall": "lerna run build",
    "buildchanges": "lerna run build --since",
    "server": "lerna run run:sockets --stream",
    "setup": "yarn && yarn buildall && yarn server"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "chaingun/packages/*"
  ],
  "name": "notabug-workspace",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
